# Environment variables — auto-populated from cloud environment via setup script
# Add new variables here as KEY=${KEY} and they'll be resolved automatically
#
# Cloud setup script (paste in Setup script field):
# ---------------------------------------------------------------
# #!/bin/bash
# cd /home/user/blog
# npm install
# node -e "
# const fs = require('fs');
# const env = fs.readFileSync('.env.example', 'utf8');
# fs.writeFileSync('.env', env.replace(/\\\$\{(\w+)\}/g, (_, k) => process.env[k] || ''));
# const mcp = fs.readFileSync('.mcp.json.example', 'utf8');
# fs.writeFileSync('.mcp.json', mcp.replace(/\\\$\{(\w+)\}/g, (_, k) => k in process.env ? process.env[k] : '\${' + k + '}'));
# "
# ---------------------------------------------------------------

AHREFS_MCP_KEY=${AHREFS_MCP_KEY}
POSTHOG_API_KEY=${POSTHOG_API_KEY}
GSC_SERVICE_ACCOUNT_JSON=${GSC_SERVICE_ACCOUNT_JSON}
